<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="StoreMapper">

	<!--表名 -->
	<sql id="tableName">
		M_STORE
	</sql>

	<!-- 字段 -->
	<sql id="Field">
		NAME,
		INTEGRAL,
		DISPLAY_ORDER,
		PATH,
		NOTICE,
		ADDRESS,
		BUSSTIME_FROM,
		BUSSTIME_TO,
		PHONE,
		OWNER,
		WECHAT_ID,
		IS_DEL,
		CREATE_TIME,
		MODIFY_TIME,
		CREATER,
		UPDATER,
		WECHAT_QRCODE,
		ALIPAY_QRCODE,
		ID,
		MCHID,
		`KEY`
	</sql>

	<!-- 字段值 -->
	<sql id="FieldValue">
		#{NAME},
		#{INTEGRAL},
		#{DISPLAY_ORDER},
		#{PATH},
		#{NOTICE},
		#{ADDRESS},
		#{BUSSTIME_FROM},
		#{BUSSTIME_TO},
		#{PHONE},
		#{OWNER},
		#{WECHAT_ID},
		#{IS_DEL},
		#{CREATE_TIME},
		#{MODIFY_TIME},
		#{CREATER},
		#{UPDATER},
		#{WECHAT_QRCODE},
		#{ALIPAY_QRCODE},
		#{ID},
		#{MCHID},
		#{KEY}
	</sql>

	<!-- 新增 -->
	<insert id="save" parameterType="pd">
		insert into
		<include refid="tableName"></include>
		(
		<include refid="Field"></include>
		) values (
		<include refid="FieldValue"></include>
		)
	</insert>

	<!-- 删除 -->
	<delete id="delete" parameterType="pd">
		delete from
		<include refid="tableName"></include>
		where
		ID = #{ID}
	</delete>

	<!-- 修改 -->
	<update id="edit" parameterType="pd">
		update
		<include refid="tableName"></include>
		set
		NAME = #{NAME},
		INTEGRAL = #{INTEGRAL},
		DISPLAY_ORDER = #{DISPLAY_ORDER},
		PATH = #{PATH},
		NOTICE = #{NOTICE},
		ADDRESS = #{ADDRESS},
		BUSSTIME_FROM = #{BUSSTIME_FROM},
		BUSSTIME_TO = #{BUSSTIME_TO},
		PHONE = #{PHONE},
		WECHAT_QRCODE = #{WECHAT_QRCODE},
		ALIPAY_QRCODE = #{ALIPAY_QRCODE},
		OWNER = #{OWNER},
		WECHAT_ID = #{WECHAT_ID},
		IS_DEL = #{IS_DEL},
		MODIFY_TIME = #{MODIFY_TIME},
		MCHID = #{MCHID},
		`KEY` = #{KEY},
		UPDATER = #{UPDATER}
		where
		ID = #{ID}
	</update>

	<!-- 通过ID获取数据 -->
	<select id="findById" parameterType="pd" resultType="pd">
		select
		<include refid="Field"></include>
		from
		<include refid="tableName"></include>
		where
		ID = #{ID}
	</select>
	<!-- 通过微信ID获取数据 -->
	<select id="findByWeichatId" parameterType="pd" resultType="pd">
		select
		<include refid="Field"></include>
		from
		<include refid="tableName"></include>
		where
		WECHAT_ID = #{WECHAT_ID}
	</select>
	<!-- 列表 -->
	<select id="datalistPage" parameterType="pd" resultType="pd">
		select
		<include refid="Field"></include>
		from
		<include refid="tableName"></include>
		where 1=1
		<if test="pd.keywords!= null and pd.keywords != ''"><!-- 关键词检索 -->
			and
			(
			<!-- 根据需求自己加检索条件 -->
			NAME LIKE CONCAT(CONCAT('%', #{pd.keywords}),'%')
			or
			NOTICE LIKE CONCAT(CONCAT('%', #{pd.keywords}),'%')
			)
		</if>
		order by DISPLAY_ORDER+0 ASC,MODIFY_TIME desc
	</select>

	<!-- 列表(全部) -->
	<select id="listAll" parameterType="pd" resultType="pd">
		select
		<include refid="Field"></include>
		from
		<include refid="tableName"></include>
		order by DISPLAY_ORDER+0 ASC,MODIFY_TIME desc
	</select>

	<!-- 批量删除 -->
	<delete id="deleteAll" parameterType="String">
		delete from
		<include refid="tableName"></include>
		where
		ID in
		<foreach item="item" index="index" collection="array" open="("
			separator="," close=")">
			#{item}
		</foreach>
	</delete>

	<!-- 列表 (Search By分类)-->
	<select id="listWebByCategory" parameterType="pd" resultType="pd">
		SELECT
		  ID
		  , NAME
		  , INTEGRAL
		  , DISPLAY_ORDER
		  , PATH
		  , NOTICE
		  , ADDRESS
		  , BUSSTIME_FROM
		  , BUSSTIME_TO
		  , PHONE
		  , OWNER
		  , WECHAT_QRCODE
		  , ALIPAY_QRCODE 
		FROM
		  M_STORE 
		WHERE
		  ID IN ( 
		    SELECT DISTINCT
		      S.ID 
		    FROM
		      M_GOODS G 
		      INNER JOIN M_STORE S 
		        ON G.STORE_ID = S.ID 
		    WHERE
		      S.IS_DEL = '0' 
		      AND G.IS_DEL = '0' 
		      AND G.CATEGORY_ID = #{CATEGORY_ID}
		  ) 
		ORDER BY
		  DISPLAY_ORDER ASC,INTEGRAL DESC
		<if test="LINEFROM != null and LINEFROM != ''">
			LIMIT #{LINEFROM}, #{LINETO}
		</if>
	</select>
	
	<!-- fh313596790qq(青苔) -->
</mapper>