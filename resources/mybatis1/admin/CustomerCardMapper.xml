<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="CustomerCardMapper">
	
	<!--表名 -->
	<sql id="tableName">
		T_CUSTOMER_CARD
	</sql>
	
	<!-- 字段 -->
	<sql id="Field">
		CARD_NO,	
		CARD_ID,	
		CUSTOMER_ID,	
		CHARGE,	
		SERVICE_CHAGE,	
		BUY_TIME,	
		BALANCE,
		SERVICE_BALANCE,
		END_TIME,	
		IS_DEL,	
		CREATE_TIME,	
		MODIFY_TIME,	
		CREATER,	
		UPDATER
	</sql>
	
	<!-- 字段值 -->
	<sql id="FieldValue">
		#{CARD_NO},	
		#{CARD_ID},	
		#{CUSTOMER_ID},	
		#{CHARGE},	
		#{SERVICE_CHAGE},	
		#{BUY_TIME},	
		#{BALANCE},	
		#{SERVICE_BALANCE},
		#{END_TIME},	
		#{IS_DEL},	
		#{CREATE_TIME},	
		#{MODIFY_TIME},	
		#{CREATER},	
		#{UPDATER}
	</sql>
	
	<!-- 新增-->
	<insert id="save" parameterType="pd">
		insert into 
	<include refid="tableName"></include>
		(
	<include refid="Field"></include>
		) values (
	<include refid="FieldValue"></include>
		)
	</insert>
	
	<!-- 删除-->
	<delete id="delete" parameterType="pd">
		delete from
		<include refid="tableName"></include>
		where 
			CUSTOMER_ID = #{ID}
	</delete>
	
	<!-- 通过Customer获取数据 -->
	<select id="findByCustomer" parameterType="pd" resultType="pd">
		select 
			A.CARD_NO,	
			A.CARD_ID,	
			A.CUSTOMER_ID,	
			A.CHARGE,	
			A.SERVICE_CHAGE,	
			DATE_FORMAT(A.BUY_TIME,'%Y-%m-%d') BUY_TIME,	
			A.BALANCE,
			A.SERVICE_BALANCE,	
			DATE_FORMAT(A.END_TIME,'%Y-%m-%d') END_TIME,	
			A.IS_DEL,	
			A.CREATE_TIME,	
			A.MODIFY_TIME,	
			A.CREATER,	
			A.UPDATER,
			B.CADR_NAME CARD_NAME
		from 
		<include refid="tableName"></include> A
		INNER JOIN M_CARD B ON A.CARD_ID=B.ID
		where CUSTOMER_ID = #{ID}
	</select>
	
	<!-- 修改 -->
	<update id="edit" parameterType="pd">
		update
		<include refid="tableName"></include>
		set 
			CARD_NO = #{CARD_NO},
			BALANCE = #{BALANCE},
			SERVICE_BALANCE = #{SERVICE_BALANCE},
			MODIFY_TIME = #{MODIFY_TIME},
			UPDATER = #{UPDATER}
		where 
		CARD_NO = #{CARD_NO}
	</update>
	
	<!-- 通过ID获取数据 -->
	<select id="findById" parameterType="pd" resultType="pd">
		select 
		<include refid="Field"></include>
		from 
		<include refid="tableName"></include>
		where 
			CARD_NO = #{CARD_NO}
	</select>
	
	<!-- 列表 -->
	<select id="datalistPage" parameterType="page" resultType="pd">
		select
		<include refid="Field"></include>
		from 
		<include refid="tableName"></include>
		where 1=1
		<if test="pd.keywords!= null and pd.keywords != ''"><!-- 关键词检索 -->
			and
				(
				<!--	根据需求自己加检索条件
					字段1 LIKE CONCAT(CONCAT('%', #{pd.keywords}),'%')
					 or 
					字段2 LIKE CONCAT(CONCAT('%', #{pd.keywords}),'%') 
				-->
				)
		</if>
	</select>
	
	<!-- 列表(全部) -->
	<select id="listAll" parameterType="pd" resultType="pd">
		select
		<include refid="Field"></include>
		from 
		<include refid="tableName"></include>
	</select>
	
	<!-- 批量删除 -->
	<delete id="deleteAll" parameterType="String">
		delete from
		<include refid="tableName"></include>
		where 
			CUSTOMER_ID in
		<foreach item="item" index="index" collection="array" open="(" separator="," close=")">
                 #{item}
		</foreach>
	</delete>
	
	<!-- fh313596790qq(青苔) -->
</mapper>