<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="CareOrderMapper">
	
	<!--表名 -->
	<sql id="tableName">
		CARE_ORDER
	</sql>
	
	<!-- 字段 -->
	<sql id="Field">
		ID,	
		CUSTOMER_ID,	
		CUSTOMER_NAME,
		CARE_DATE_START,	
		CATE_DATE_END,	
		STAFF_NAME,	
		CARE_ITEM_ID,	
		CATE_ITEM_NAME,	
		SHOP_ID,	
		SHOP_NAME,	
		PRICE_ORI,	
		PRICE_DISCUT,	
		SETTLE_TYPE,	
		PRESENT1,	
		PRESENT2,	
		PRESENTFREE,
		IMAGE_ID
	</sql>
	
	<sql id="Field2">
		'2' TYPE,
		CUSTOMER_ID,	
		CONCAT(CUSTOMER_NAME ,'  技师：',STAFF_NAME,' 项目：',CATE_ITEM_NAME) title, 
		'#4876FF' color,
		date_format(CARE_DATE_START, '%Y-%m-%d %H:%i:%S') start,	
		date_format(CATE_DATE_END, '%Y-%m-%d %H:%i:%S') end,	
		STAFF_NAME,	
		CATE_ITEM_NAME,	
		SHOP_ID,
		SHOP_NAME,	
		PRICE_ORI,	
		PRICE_DISCUT,	
		SETTLE_TYPE,	
		PRESENT1,	
		PRESENT2,	
		PRESENTFREE,	
		CARE_ITEM_ID,	
		ID
	</sql>
	<!-- 字段值 -->
	<sql id="FieldValue">
		#{ID},	
		#{CUSTOMER_ID},	
		#{CUSTOMER_NAME},
		#{CARE_DATE_START},	
		#{CATE_DATE_END},	
		#{STAFF_NAME},	
		#{CARE_ITEM_ID},	
		#{CATE_ITEM_NAME},	
		#{SHOP_ID},	
		#{SHOP_NAME},	
		#{PRICE_ORI},	
		#{PRICE_DISCUT},	
		#{SETTLE_TYPE},	
		#{PRESENT1},	
		#{PRESENT2},	
		#{PRESENTFREE},
		#{IMAGE_ID}
	</sql>
	
	<!-- 新增-->
	<insert id="save" parameterType="pd">
		insert into 
	<include refid="tableName"></include>
		(
	<include refid="Field"></include>
		) values (
	<include refid="FieldValue"></include>
		)
	</insert>
	
	<!-- 删除-->
	<delete id="delete" parameterType="pd">
		delete from
		<include refid="tableName"></include>
		where 
			ID = #{ID}
	</delete>
	
	<!-- 修改 -->
	<update id="edit" parameterType="pd">
		update
		<include refid="tableName"></include>
		set 
			ID = #{ID},
			CUSTOMER_ID = #{CUSTOMER_ID},
			CUSTOMER_NAME = #{CUSTOMER_NAME},
			CARE_DATE_START = #{CARE_DATE_START},
			CATE_DATE_END = #{CATE_DATE_END},
			STAFF_NAME = #{STAFF_NAME},
			CARE_ITEM_ID = #{CARE_ITEM_ID},
			CATE_ITEM_NAME = #{CATE_ITEM_NAME},
			SHOP_ID = #{SHOP_ID},
			SHOP_NAME = #{SHOP_NAME},
			PRICE_ORI = #{PRICE_ORI},
			PRICE_DISCUT = #{PRICE_DISCUT},
			SETTLE_TYPE = #{SETTLE_TYPE},
			PRESENT1 = #{PRESENT1},
			PRESENT2 = #{PRESENT2},
			PRESENTFREE = #{PRESENTFREE},
			IMAGE_ID = #{IMAGE_ID}
		where 
		ID = #{ID}
	</update>
	
	<!-- 通过ID获取数据 -->
	<select id="findById" parameterType="pd" resultType="pd">
		select 
		<include refid="Field"></include>
		from 
		<include refid="tableName"></include>
		where 
			ID = #{ID}
	</select>
	
	<!-- 列表 -->
	<select id="datalistPage" parameterType="page" resultType="pd">
		select
		<include refid="Field"></include>
		from 
		<include refid="tableName"></include>
		where 1=1
		<if test="pd.keywords!= null and pd.keywords != ''"><!-- 关键词检索 -->
			and
				(
				<!--	根据需求自己加检索条件
					字段1 LIKE CONCAT(CONCAT('%', #{pd.keywords}),'%')
					 or 
					字段2 LIKE CONCAT(CONCAT('%', #{pd.keywords}),'%') 
				-->
				)
		</if>
	</select>
	
	<!-- 日程列表 -->
	<select id="datalistPage_time" parameterType="page" resultType="pd">
		select
		<include refid="Field2"></include>
		from 
		<include refid="tableName"></include>
		where 1=1
		<if test="pd.start != null">
	<![CDATA[		 AND CARE_DATE_START >= #{pd.start}]]>
		</if>
        
		<if test="pd.end != null">
			<![CDATA[		  AND CARE_DATE_START <= #{pd.end}]]>
		</if>
	</select>
	<!-- 列表(全部) -->
	<select id="listAll" parameterType="pd" resultType="pd">
		select
		<include refid="Field"></include>
		from 
		<include refid="tableName"></include>
	</select>
	
	<!-- 批量删除 -->
	<delete id="deleteAll" parameterType="String">
		delete from
		<include refid="tableName"></include>
		where 
			ID in
		<foreach item="item" index="index" collection="array" open="(" separator="," close=")">
                 #{item}
		</foreach>
	</delete>
	
	<!-- fh313596790qq(青苔) -->
</mapper>