<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="GoodsMapper">

	<!--表名 -->
	<sql id="tableName">
		M_GOODS
	</sql>

	<!-- 字段 -->
	<sql id="Field">
		NAME,
		DETAILS,
		PATH,
		PRICE,
		UNIT,
		DISPLAY_ORDER,
		STORE_ID,
		CATEGORY_ID,
		CATEGORY_ID_1,
		CATEGORY_ID_2,
		IS_DEL,
		CREATE_TIME,
		MODIFY_TIME,
		CREATER,
		UPDATER,
		ID
	</sql>

	<!-- 字段值 -->
	<sql id="FieldValue">
		#{NAME},
		#{DETAILS},
		#{PATH},
		#{PRICE},
		#{UNIT},
		#{DISPLAY_ORDER},
		#{STORE_ID},
		#{CATEGORY_ID},
		#{CATEGORY_ID_1},
		#{CATEGORY_ID_2},
		#{IS_DEL},
		#{CREATE_TIME},
		#{MODIFY_TIME},
		#{CREATER},
		#{UPDATER},
		#{ID}
	</sql>

	<!-- 新增 -->
	<insert id="save" parameterType="pd">
		insert into
		<include refid="tableName"></include>
		(
		<include refid="Field"></include>
		) values (
		<include refid="FieldValue"></include>
		)
	</insert>

	<!-- 删除 -->
	<delete id="delete" parameterType="pd">
		delete from
		<include refid="tableName"></include>
		where
		ID = #{ID}
	</delete>

	<!-- 修改 -->
	<update id="edit" parameterType="pd">
		update
		<include refid="tableName"></include>
		set
		NAME = #{NAME},
		DETAILS = #{DETAILS},
		PATH = #{PATH},
		PRICE = #{PRICE},
		UNIT = #{UNIT},
		DISPLAY_ORDER = #{DISPLAY_ORDER},
		CATEGORY_ID = #{CATEGORY_ID},
		CATEGORY_ID_1 = #{CATEGORY_ID_1},
		CATEGORY_ID_2 = #{CATEGORY_ID_2},
		IS_DEL = #{IS_DEL},
		MODIFY_TIME = #{MODIFY_TIME},
		UPDATER = #{UPDATER}
		where
		ID = #{ID}
	</update>

	<!-- 通过ID获取数据 -->
	<select id="findById" parameterType="pd" resultType="pd">
		select
		<include refid="Field"></include>
		from
		<include refid="tableName"></include>
		where
		ID = #{ID}
	</select>

	<!-- 列表 -->
	<select id="datalistPage" parameterType="page" resultType="pd">
		select
		<include refid="Field"></include>
		from
		<include refid="tableName"></include>
		where 1 = 1
		<if test="pd.STORE_ID!= null and pd.STORE_ID != ''">
			and STORE_ID = #{pd.STORE_ID}
		</if>
		<if test="pd.keywords!= null and pd.keywords != ''"><!-- 关键词检索 -->
			and
			(
			<!-- 根据需求自己加检索条件 -->
			NAME LIKE CONCAT(CONCAT('%', #{pd.keywords}),'%')
			or
			DETAILS LIKE CONCAT(CONCAT('%', #{pd.keywords}),'%')

			)
		</if>
		order by DISPLAY_ORDER+0 ASC,MODIFY_TIME desc
	</select>

	<!-- 列表(全部) -->
	<select id="listAll" parameterType="pd" resultType="pd">
		select
		<include refid="Field"></include>
		from
		<include refid="tableName"></include>
	</select>

	<!-- 批量删除 -->
	<delete id="deleteAll" parameterType="String">
		delete from
		<include refid="tableName"></include>
		where
		ID in
		<foreach item="item" index="index" collection="array" open="("
			separator="," close=")">
			#{item}
		</foreach>
	</delete>

	<!-- 列表(Search ByIDList) -->
	<select id="listByidlist" parameterType="String" resultType="pd">
		SELECT
			ID
			,NAME
			,DETAILS
			,PATH
			,PRICE
			,UNIT
			,DISPLAY_ORDER
			,STORE_ID
			,CATEGORY_ID
			,IS_DEL
			,CREATE_TIME
			,MODIFY_TIME
			,CREATER
			,UPDATER
			,CATEGORY_ID_1
			,CATEGORY_ID_2
		FROM
		  M_GOODS 
		WHERE
			ID in
		<foreach item="item" index="index" collection="array" open="("
			separator="," close=")">
			#{item}
		</foreach>
	</select>
	
	<!-- 列表(Search By店铺ID) -->
	<select id="listWebByStore" parameterType="pd" resultType="pd">
		SELECT
		  ID
		  , NAME
		  , DETAILS
		  , PATH
		  , PRICE
		  , UNIT
		  , STORE_ID
		  , CATEGORY_ID
		  , CATEGORY_ID_1
		  , CATEGORY_ID_2 
		FROM
		  M_GOODS 
		WHERE
		  STORE_ID = #{STORE_ID}
		  AND IS_DEL = '0'
		ORDER BY
		  DISPLAY_ORDER
		<if test="LINEFROM != null and LINEFROM != ''">
			LIMIT #{LINEFROM}, #{LINETO}
		</if>
	</select>
	
	<!-- fh313596790qq(青苔) -->
</mapper>